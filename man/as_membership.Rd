% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{as_membership}
\alias{as_membership}
\title{Community membership of a partition}
\usage{
as_membership(x, singleton_na = FALSE)
}
\arguments{
\item{x}{An \code{"icons_partition"} object from \code{\link[=icons_detect]{icons_detect()}}.}

\item{singleton_na}{Logical; label singletons \code{NA} instead of \code{0}.}
}
\value{
An integer vector of length \code{p}, named with the variable names when
they are available.
}
\description{
Extracts the community label of every variable, in the original variable
order.  Unassigned (singleton) variables get \code{0}, or \code{NA} if you prefer to
treat them as missing.
}
\examples{
data(sim)
part <- icons_detect(cor(sim), threshold = 0.6)
table(as_membership(part))

}
