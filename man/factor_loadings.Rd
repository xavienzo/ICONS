% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scfa-methods.R
\name{factor_loadings}
\alias{factor_loadings}
\title{Factor loading matrix of an SCFA fit}
\usage{
factor_loadings(object, sparse = FALSE)
}
\arguments{
\item{object}{An \code{"scfa"} object.}

\item{sparse}{Logical; return a two-column \code{variable}/\code{factor} index instead
of the dense matrix.  Useful when \code{p} is large.}
}
\value{
A \code{p} by \code{K} matrix with one non-zero entry per modelled row, or a
data frame when \code{sparse = TRUE}.
}
\description{
Builds \eqn{\hat L = \mathrm{Bdiag}(1_{p_1}, \ldots, 1_{p_K})} in the
original variable order.  It is not stored with the fit because the
membership determines it exactly, and at large \code{p} this matrix would dwarf
everything else in the object.
}
\details{
Named \code{factor_loadings()} rather than \code{loadings()} because \code{stats::loadings}
is not a generic --- it is defined as \code{x$loadings} --- so no S3 method for
it is possible, and masking it would be worse than a distinct name.
}
\examples{
data(sim)
fit <- scfa(sim, icons_detect(cor(sim), threshold = 0.6))
dim(factor_loadings(fit))
head(factor_loadings(fit, sparse = TRUE))

}
