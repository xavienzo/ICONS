% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scfa-methods.R
\name{fitted.scfa}
\alias{fitted.scfa}
\title{Model-implied covariance matrix}
\usage{
\method{fitted}{scfa}(object, max_p = 5000L, ...)
}
\arguments{
\item{object}{An \code{"scfa"} object.}

\item{max_p}{Guard rail: refuse when \code{p} exceeds this. Defaults to 5000,
about 200 MB.}

\item{...}{Ignored.}
}
\value{
A \code{p} by \code{p} matrix in the original variable order.
}
\description{
Reconstructs \eqn{\hat\Sigma = \hat L\hat\Sigma_f\hat L^\top + \hat\Sigma_u}
explicitly.  This is the one function in the package that materialises a
\eqn{p \times p} matrix, so it refuses to run for large \code{p} unless you ask
it to.
}
\examples{
data(sim)
fit <- scfa(sim, icons_detect(cor(sim), threshold = 0.6))
dim(fitted(fit))

}
