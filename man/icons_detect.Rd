% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{icons_detect}
\alias{icons_detect}
\title{Detect interconnected communities in a covariance matrix}
\usage{
icons_detect(
  W,
  threshold = 0.5,
  probs = NULL,
  lambda = 0.5,
  min_size = 2L,
  max_k = Inf
)
}
\arguments{
\item{W}{A square symmetric numeric matrix, typically \code{cor(data)} or
\code{cov(data)}.  The diagonal is ignored.}

\item{threshold}{Numeric edge threshold; weights below it are set to zero.
Ignored when \code{probs} is supplied.  Defaults to \code{0.5}.}

\item{probs}{Optional single probability in \eqn{(0, 1)}.  When given, the
threshold is the corresponding quantile of the strict upper triangle of
\code{W}, e.g. \code{probs = 0.95} keeps the strongest 5\% of edges.}

\item{lambda}{Shrinkage exponent \eqn{\lambda} in \eqn{[0, 1]}.  Defaults to
\code{0.5}.}

\item{min_size}{Smallest community retained, at least \code{2}.  Anything smaller
goes to the singleton set.  SCFA requires at least two variables per
factor, so \code{2} is the lowest usable value.  Defaults to \code{2}.}

\item{max_k}{Optional cap on the number of communities.  \code{Inf} (the default)
means no cap.}
}
\value{
An object of class \code{"icons_partition"}: a list with components
\describe{
\item{\code{sizes}}{Integer vector of community sizes, largest-first by
extraction order.}
\item{\code{order}}{Integer permutation of \code{seq_len(ncol(W))}: community
members in block order, then the singleton set.}
\item{\code{membership}}{Integer vector of length \code{ncol(W)} giving the
community of each \emph{original} variable, \code{0} for singletons.}
\item{\code{n_singletons}}{Number of unassigned variables.}
\item{\code{scores}}{Value of the peeling objective for each community.}
\item{\code{threshold}, \code{lambda}, \code{min_size}}{The settings used.}
}
}
\description{
Repeatedly applies \code{\link[=greedy_peeling]{greedy_peeling()}}: the densest subgraph is extracted and
removed, the algorithm runs again on what is left, and so on until the
densest remaining subgraph falls below \code{min_size}.  Variables never assigned
to a community form the \emph{singleton set}, which is reported separately rather
than treated as another community.
}
\details{
The resulting partition specifies the non-zero loadings of the
semi-confirmatory factor model fitted by \code{\link[=scfa]{scfa()}}; each community becomes one
common factor.

\code{threshold} is the single most influential argument: it decides which edges
the peeling sees at all.  Supply it as a quantile of the observed edge
weights via \code{probs}, or tune it together with \code{lambda} using \code{\link[=icons_tune]{icons_tune()}}.
}
\examples{
data(sim)
W <- cor(sim)

part <- icons_detect(W, threshold = 0.6)
part

# Threshold given as a quantile of the edge weights instead
icons_detect(W, probs = 0.95)

}
\references{
Wu, Q., Huang, X., Culbreth, A. J., Waltz, J. A., Hong, L. E., & Chen, S.
(2022). Extracting brain disease-related connectome subgraphs by adaptive
dense subgraph discovery. \emph{Biometrics}, 78(4), 1566--1578.
\doi{10.1111/biom.13537}

Chen, S., Zhang, Y., Wu, Q., Bi, C., Kochunov, P., & Hong, L. E. (2024).
Identifying covariate-related subnetworks for whole-brain connectome
analysis. \emph{Biostatistics}, 25(2), 541--558.
\doi{10.1093/biostatistics/kxad007}
}
\seealso{
\code{\link[=icons_tune]{icons_tune()}} to choose \code{threshold} and \code{lambda},
\code{\link[=scfa]{scfa()}} to fit the factor model, \code{\link[=reorder_matrix]{reorder_matrix()}} and \code{\link[=plot_matrix]{plot_matrix()}}
to visualise the result.
}
