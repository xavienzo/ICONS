% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nfactors.R
\name{n_factors}
\alias{n_factors}
\title{Choose the number of factors}
\usage{
n_factors(data, partition, k_max = NULL, criterion = c("frobenius", "legacy"))
}
\arguments{
\item{data}{A numeric matrix or data frame, observations in rows.}

\item{partition}{An \code{"icons_partition"} from \code{\link[=icons_detect]{icons_detect()}}, or a
membership vector.}

\item{k_max}{Largest number of factors to consider.  Defaults to every
community in \code{partition}.}

\item{criterion}{\code{"frobenius"} (default) uses the fast incremental path;
\code{"legacy"} refits the 0.1.x objective at every \code{k} and is much slower.}
}
\value{
An object of class \code{"icons_nfactors"}:
\describe{
\item{\code{path}}{Data frame with \code{k}, \code{criterion}, \code{relative}, \code{p_modelled}
and \code{n_singletons}.}
\item{\code{suggested}}{Elbow of the curve: the \code{k} furthest from the chord
joining its endpoints.}
}
}
\description{
Traces the fit criterion as communities are admitted to the model one at a
time, largest first, and finds the elbow.  With \eqn{k = 0} no variable
loads on a factor and the criterion equals \eqn{\|\mathrm{offdiag}(S)\|_F};
each additional community explains more covariance, with diminishing
returns once the real communities are used up.
}
\section{Cost}{

The whole path costs one \eqn{O(n^2p)} pass plus \eqn{O(K^3)}, not \eqn{K}
separate fits.  The block summaries that the estimators depend on are
computed once for the full partition, and the sub-model for \eqn{k}
communities reuses the leading sub-blocks unchanged --- \eqn{\hat a_{kk}}
and \eqn{\hat b_{kk'}} depend only on their own blocks, so admitting further
communities never alters them.
}

\examples{
data(sim)
part <- icons_detect(cor(sim), threshold = 0.6)
nf <- n_factors(sim, part)
nf
plot(nf)

}
\seealso{
\code{\link[=icons_detect]{icons_detect()}}, \code{\link[=scfa]{scfa()}}, \code{\link[=scfa_criterion]{scfa_criterion()}}
}
