% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scfa-methods.R
\name{residuals.scfa}
\alias{residuals.scfa}
\title{Residuals of an SCFA fit}
\usage{
\method{residuals}{scfa}(object, data, ...)
}
\arguments{
\item{object}{An \code{"scfa"} object.}

\item{data}{The data the model was fitted to.}

\item{...}{Ignored.}
}
\value{
An \code{n} by \code{p} matrix.  Singleton columns are returned centred but
otherwise untouched, since no factor was fitted to them.
}
\description{
The unexplained part \eqn{U = X_c - \hat F \hat L^\top}.  The data are
required because the fit does not retain them.
}
\examples{
data(sim)
fit <- scfa(sim, icons_detect(cor(sim), threshold = 0.6))
U <- residuals(fit, sim)
round(range(colMeans(U)), 10)

}
