% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{reorder_matrix}
\alias{reorder_matrix}
\title{Reorder a matrix by a detected partition}
\usage{
reorder_matrix(W, partition, communities_only = FALSE)
}
\arguments{
\item{W}{A square matrix with the same dimension as the matrix that produced
\code{partition}.}

\item{partition}{An \code{"icons_partition"} object.}

\item{communities_only}{Logical; drop the singleton set.}
}
\value{
The reordered matrix.
}
\description{
Permutes rows and columns so community members are adjacent, which is what
makes the block structure visible in a heat map.
}
\examples{
data(sim)
W <- cor(sim)
part <- icons_detect(W, threshold = 0.6)
plot_matrix(reorder_matrix(W, part), partition = part)

}
