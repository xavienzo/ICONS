% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{greedy_peeling}
\alias{greedy_peeling}
\title{Greedy peeling with \eqn{\ell_0} shrinkage}
\usage{
greedy_peeling(W, lambda = 0.5, threshold = -Inf)
}
\arguments{
\item{W}{A square symmetric numeric matrix of edge weights, typically a
correlation or covariance matrix.  The diagonal is ignored.}

\item{lambda}{Shrinkage exponent \eqn{\lambda}, a single number in
\eqn{[0, 1]}.  Defaults to \code{0.5}.}

\item{threshold}{Edge weights below \code{threshold} are set to zero before
peeling.  Defaults to \code{-Inf}, which keeps every edge.}
}
\value{
A list with components
\describe{
\item{\code{keep}}{Integer node indices of the extracted subgraph, ordered
from the densest core outwards.}
\item{\code{drop}}{Integer node indices peeled away before the best cut.}
\item{\code{score}}{The value of \eqn{h_\lambda} at the best cut.}
}
}
\description{
Extracts a single dense subgraph.  Nodes are stripped one at a time, always
the one with the smallest remaining weighted degree, and the surviving set
is scored by the generalised density
\deqn{h_\lambda(S) = w(S) / |S|^{2\lambda},}
where \eqn{w(S)} is the total edge weight inside \eqn{S}.  The best-scoring
set over the whole peeling sequence is returned.  \eqn{\lambda = 1/2}
recovers the average-degree objective of Charikar (2000); larger
\eqn{\lambda} penalises size more heavily and yields smaller, denser
subgraphs.
}
\details{
This is the inner step of \code{\link[=icons_detect]{icons_detect()}}, which calls it repeatedly.  Use
\code{icons_detect()} unless you specifically want one subgraph.
}
\examples{
data(sim)
W <- cor(sim)
res <- greedy_peeling(W, lambda = 0.6, threshold = 0.5)
length(res$keep)

}
\references{
Charikar, M. (2000). Greedy approximation algorithms for finding dense
components in a graph. \emph{Approximation Algorithms for Combinatorial
Optimization}, 84--95.

Chen, S., Zhang, Y., Wu, Q., Bi, C., Kochunov, P., & Hong, L. E. (2024).
Identifying covariate-related subnetworks for whole-brain connectome
analysis. \emph{Biostatistics}, 25(2), 541--558.
\doi{10.1093/biostatistics/kxad007}

Tsourakakis, C., Bonchi, F., Gionis, A., Gullo, F., & Tsiarli, M. (2013).
Denser than the densest subgraph: extracting optimal quasi-cliques with
quality guarantees. \emph{KDD '13}, 104--112. \doi{10.1145/2487575.2487645}
}
\seealso{
\code{\link[=icons_detect]{icons_detect()}}
}
