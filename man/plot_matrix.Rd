% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_matrix}
\alias{plot_matrix}
\title{Heat map of a matrix}
\usage{
plot_matrix(
  z,
  partition = NULL,
  zlim = NULL,
  col = NULL,
  max_cells = 2000L,
  border = "grey20",
  file = NULL,
  format = NULL,
  width = 2500,
  height = 2350,
  res = 300,
  main = NULL,
  xlab = "",
  ylab = "",
  ...
)
}
\arguments{
\item{z}{A numeric matrix.  Need not be square.}

\item{partition}{Optional \code{"icons_partition"}.  When given, \code{z} is assumed
to be already reordered by \code{\link[=reorder_matrix]{reorder_matrix()}} and community boundaries are
drawn.}

\item{zlim}{Numeric length-2 colour-bar range.  Defaults to \code{range(z)}.
Fix it across plots to make them comparable.}

\item{col}{Colour palette.  Defaults to a jet-like ramp of 256 colours.}

\item{max_cells}{Largest matrix side drawn at full resolution; larger
matrices are block-averaged using the smallest whole-number block size that
brings them under this. Set to \code{Inf} to disable.  Axis labels always refer
to original variable indices, whether or not downsampling happened.}

\item{border}{Colour of the community boundary lines, or \code{NA} for none.}

\item{file}{Optional path to save to instead of drawing on the current
device.  The extension picks the format unless \code{format} is given.}

\item{format}{One of \code{"tiff"}, \code{"png"}, \code{"jpeg"}, \code{"pdf"}, \code{"eps"}, \code{"svg"}.}

\item{width, height}{Size of the saved image, pixels for raster formats and
inches for vector ones.}

\item{res}{Resolution in ppi for raster formats.}

\item{main, xlab, ylab}{Titles.}

\item{...}{Passed to \code{\link[graphics:image]{graphics::image()}}.}
}
\value{
\code{NULL}, invisibly.  Called for the plot.
}
\description{
Draws a matrix the way \code{imagesc} does in MATLAB: row 1 at the top, a jet
colour ramp, and a colour bar on the right.  Optionally overlays the
community boundaries of a partition.
}
\details{
Unlike ICONS 0.1.x this restores the graphics state on exit, so the device
is not left in a two-panel layout, and it downsamples very large matrices
rather than pushing millions of cells at a device that cannot resolve them.
}
\examples{
data(sim)
W <- cor(sim)
part <- icons_detect(W, threshold = 0.6)

plot_matrix(W, main = "Original")
plot_matrix(reorder_matrix(W, part), partition = part, main = "Reordered")

}
