% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune.R
\name{icons_tune}
\alias{icons_tune}
\title{Tune the detection threshold and shrinkage exponent}
\usage{
icons_tune(
  W,
  data,
  probs = seq(0.9, 0.99, by = 0.01),
  lambda = seq(0.4, 0.8, length.out = 5),
  criterion = c("frobenius", "legacy"),
  min_size = 2L,
  ncores = getOption("ICONS.ncores", 1L),
  verbose = FALSE
)
}
\arguments{
\item{W}{A square symmetric numeric matrix, typically \code{cor(data)}.}

\item{data}{The numeric data matrix \code{W} was computed from, observations in
rows.}

\item{probs}{Numeric vector of quantiles of the edge weights to use as
thresholds.  Defaults to \code{seq(0.90, 0.99, by = 0.01)}.}

\item{lambda}{Numeric vector of shrinkage exponents.  Defaults to
\code{seq(0.4, 0.8, length.out = 5)}.}

\item{criterion}{Fit criterion, \code{"frobenius"} (default) or \code{"legacy"}; see
\code{\link[=scfa_criterion]{scfa_criterion()}}.}

\item{min_size}{Smallest retained community, passed to \code{\link[=icons_detect]{icons_detect()}}.}

\item{ncores}{Number of worker processes.  Defaults to
\code{getOption("ICONS.ncores", 1L)}; \code{1} runs sequentially.}

\item{verbose}{Logical; report progress.}
}
\value{
An object of class \code{"icons_tune"}:
\describe{
\item{\code{best}}{One-row data frame with the selected settings.}
\item{\code{grid}}{Data frame with one row per candidate: \code{probs},
\code{threshold}, \code{lambda}, \code{k} (communities found), \code{n_singletons},
\code{criterion}, \code{relative}.}
\item{\code{threshold}, \code{lambda}}{The selected values, for passing straight
to \code{\link[=icons_detect]{icons_detect()}}.}
}
}
\description{
Grid search over the edge threshold and \eqn{\lambda}.  Every candidate pair
is run through \code{\link[=icons_detect]{icons_detect()}} and \code{\link[=scfa]{scfa()}}, and the pair minimising the
fit criterion is returned.
}
\details{
The threshold matters far more than \eqn{\lambda}: it decides which edges
the peeling ever sees, so it is specified as a grid of quantiles of the
observed edge weights, which adapts to the scale of the data.
}
\section{Interpreting the result}{

The criterion is evaluated on the same matrix used to select the parameters,
so it measures fit, not out-of-sample performance, and the minimised value
is optimistically biased.  Treat the selected \code{threshold}/\code{lambda} as a
data-driven default and the criterion surface --- \code{plot()} on the result ---
as the more informative output: a flat surface means the choice is not
well identified.
}

\examples{
data(sim)
W <- cor(sim)
tuned <- icons_tune(W, sim, probs = c(0.90, 0.95), lambda = c(0.5, 0.7))
tuned

part <- icons_detect(W, threshold = tuned$threshold, lambda = tuned$lambda)

}
\seealso{
\code{\link[=icons_detect]{icons_detect()}}, \code{\link[=scfa]{scfa()}}, \code{\link[=n_factors]{n_factors()}}
}
