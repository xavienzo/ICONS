% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{ICONS-deprecated}
\alias{ICONS-deprecated}
\alias{dense}
\alias{param_tuning_sigmau}
\alias{k.elbow}
\alias{plotMatrix}
\alias{get_membership}
\alias{get_index}
\alias{get_vectorform}
\title{Deprecated functions from ICONS 0.1.x}
\usage{
dense(W_original, threshold = 0.5, lambda = 0.5)

param_tuning_sigmau(
  Wp,
  data,
  prctile_vec,
  lam_vec,
  method = "Sample",
  ncores = NULL,
  use_parallel = TRUE
)

k.elbow(data, CID, Clist, k = length(CID), method = "Sample", epsilon = 1e-06)

plotMatrix(
  z,
  filepath = NULL,
  save.image = FALSE,
  width = 2500,
  height = 2350,
  res = 300,
  format = "tiff",
  colorbar.range = NULL,
  ...
)

get_membership(CID, Clist)

get_index(blockid, CID, Clist)

get_vectorform(dist)
}
\arguments{
\item{W_original, Wp}{Adjacency matrix.}

\item{threshold, lambda}{Detection settings.}

\item{data}{Data matrix.}

\item{prctile_vec}{Percentiles (0-100) for the threshold grid.}

\item{lam_vec}{Lambda grid.}

\item{method}{Ignored; retained for signature compatibility.}

\item{ncores, use_parallel}{Parallel settings.}

\item{CID}{Community sizes.}

\item{Clist}{Reordered variable indices.}

\item{k}{Maximum number of factors.}

\item{epsilon}{Ignored; the closed-form estimators need no regularisation.}

\item{z}{Matrix to plot.}

\item{filepath, save.image, width, height, res, format, colorbar.range}{Plot
arguments.}

\item{...}{Passed through.}

\item{blockid}{Block numbers.}

\item{dist}{Symmetric matrix.}
}
\value{
The same shapes ICONS 0.1.x returned.
}
\description{
These keep scripts written against ICONS 0.1.x running.  Each returns the
old shape of output and warns once per session.  They will be removed in a
future release.
}
\details{
\tabular{ll}{
   0.1.x \tab 0.2.0 \cr
   \code{dense()} \tab \code{\link[=icons_detect]{icons_detect()}} \cr
   \code{param_tuning_sigmau()} \tab \code{\link[=icons_tune]{icons_tune()}} \cr
   \code{k.elbow()} \tab \code{\link[=n_factors]{n_factors()}} \cr
   \code{plotMatrix()} \tab \code{\link[=plot_matrix]{plot_matrix()}} \cr
   \code{get_membership()} \tab \code{\link[=as_membership]{as_membership()}} \cr
   \code{get_index()} \tab \code{\link[=block_index]{block_index()}} \cr
   \code{get_vectorform()} \tab \code{\link[=half_vec]{half_vec()}} \cr
}


\code{scfa()} kept its name but changed signature: it now takes a partition
object (or membership vector) instead of the \code{CID}/\code{Clist} pair, returns
the closed-form maximum likelihood estimators of Yang, Ma, Bi and Chen
(2024) rather than \code{cov(F_hat)}, and no longer builds \code{p} by \code{p} residual
matrices.  See \code{vignette("ICONS")} for the migration.
}
\keyword{internal}
